name: Template CI/CD Pipeline

on:
  push:
    branches: [main, saas-admin]
  pull_request:
    branches: [main, saas-admin]
  workflow_dispatch:

jobs:
  template-validation:
    name: Template Validation
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Template Validation
        run: |
          echo "🎯 TEMPLATE VALIDATION STARTED"
          echo "📊 Timestamp: $(date)"
          echo "🔄 This is a GitHub template validation"
          echo "✅ No Storybook, no Vercel, no deployment"
          echo "🚀 Turborepo CI/CD only"
      
      - name: Run Turborepo Lint
        run: npx turbo lint
      
      - name: Run Turborepo Type Check
        run: npx turbo type-check
      
      - name: Run Turborepo Tests
        run: npx turbo test
      
      - name: Run Turborepo Build
        run: npx turbo build
      
      - name: Template Success
        run: |
          echo "🎉 TEMPLATE VALIDATION COMPLETE"
          echo "✅ All checks passed"
          echo "🚀 Ready for use as GitHub template"
